if [ -n "${m_SHELL_ENV-}" ] && [ -r "$m_SHELL_ENV" ]
then
  . "$m_SHELL_ENV"
fi

unset m_SHELL_ENV

. "$m_LIB_DIR/sh/core.lib.sh"

PS1="${m_OS_NAME}:${m_SHELL_NAME} $(hostname):\$(pwd)\$ "

if [ -f "$m_SHELL_EXT" ] && [ -r "$m_SHELL_EXT" ]
then
  . "$m_SHELL_EXT"
fi
